<div class="main posts-new">
<div class="container">
  <div class="form-heading">イラスト</div>

    <%= form_with model: @image do |form| %>
      <% if @image.errors.any? %>
        <div style="color: red">
          <h2><%= pluralize(image.errors.count, "error") %> prohibited this movie from being saved:</h2>

          <ul>
            <% @image.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>

        </div>
      <% end %>
    <% end %>

  <div class="form">
  <div class="form-body">
    <%= form_with model: @image, local: true do |f| %>

      <%= f.label :イラスト %>
      <%= f.file_field :ctype, accept: "image/*" %>
      
      <div>
        <%= f.label :タイトル %>
          <%= f.text_field :image_name, :size => 30 %>
      </div>
      <div>
        <%= f.label :概要欄 %>
          <%= f.text_area :body, :size => 300 %>
      </div>
      <div class="form-group">
        <%= f.submit "投稿" %>
      </div>
    <% end %>
  </div>
  </div>
</div>
</div>
